---
- name: Module 1 Solve — install htop package on bastion
  hosts: localhost
  connection: local
  gather_facts: false
  become: true
  tasks:
    - name: Install htop package
      ansible.builtin.package:
        name: htop
        state: present
